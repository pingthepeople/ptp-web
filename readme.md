# IGA tracker website
A laravel app

## Set up
edit `.env` with `DB_` connection info, then run the following

```
# install dependencies
composer install
# create database tables
php artisan migrate
```
